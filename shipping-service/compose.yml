services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=shipping_service'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5435:5432'
