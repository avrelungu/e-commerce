services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=order_service'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5432:5432'
