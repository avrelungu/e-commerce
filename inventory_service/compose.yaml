services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=inventory_service'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=myuser'
    ports:
      - '5433:5432'
